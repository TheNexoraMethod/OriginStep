{
  "name": "originstep",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "dev:web": "npm run dev --workspace=apps/web"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "~5.9.2"
  },
  "engines": {
    "node": ">=20"
  }
}
